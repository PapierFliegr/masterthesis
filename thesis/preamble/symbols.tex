\newlist{abbrv}{itemize}{1}
\setlist[abbrv,1]{
	label =,
	labelwidth = 1in,
	align = parleft,
	itemsep = 0.1\baselineskip,
	leftmargin =!}

\def\symbolentry#1#2#3{\item[#2] #3}

\newcommand{\listofsymbols}{
	\chapter*{List of symbols}
  Any set considered in this thesis is assumed to be Lebesgue measurable and has at least Lipschitz
  boundary.
	\begin{abbrv}

		\symbolentry{Rn}{\( \mathbb{R}^n \)}{Euclidean space of dimension \( n \)}
    \symbolentry{Hausdorff}{\( \mathcal{H}^{n} \)}{Hausdorff measure on \( \mathbb{R}^{n} \)}
    \symbolentry{Brc}{\( B_{r} \)}{Open ball of radius \( r \) centered at the origin}
    \symbolentry{Br}{\( B_r(x) \)}{Open ball of radius \( r \) centered at \( x \)}
    \symbolentry{B'}{\( B^{\prime}_{r}(x) \)}{Open Ball of radius \( r \) centered at \( x \) in \( \mathbb{R}^{n-1}  \) }
		\symbolentry{dist}{\( \dist(A,B) \)}{Distance between sets \( A \) and \( B \)}
    
	\end{abbrv}}
